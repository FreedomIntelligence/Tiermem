# Core dependencies
openai>=1.0.0
anthropic>=0.18.0
qdrant-client>=1.9.0
pydantic>=2.0.0

# Embeddings and ML
numpy>=1.24.0
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0
fastembed>=0.2.0

# Vector stores and search
tantivy>=0.22.0  # For BM25 search

# LLM and inference
vllm>=0.4.0  # Optional, for router serving
tiktoken>=0.5.0

# Data processing
pandas>=2.0.0
datasets>=2.14.0  # For loading benchmark datasets

# API and utilities
fastapi>=0.104.0  # For mem0 service
uvicorn>=0.24.0
httpx>=0.25.0
tenacity>=8.2.0  # Retry logic
pytz>=2023.3

# JSON and parsing
json-repair>=0.5.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Async support
aiohttp>=3.9.0
aiofiles>=23.0.0

# Logging and monitoring
loguru>=0.7.0
tqdm>=4.66.0

# Optional: Training dependencies (uncomment if training router)
# ms-swift>=2.4.0
# deepspeed>=0.12.0
# wandb>=0.16.0
# accelerate>=0.25.0
# peft>=0.7.0
# bitsandbytes>=0.41.0
